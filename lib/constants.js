/*
 * Module that defines constants used in the hvac app.
 * freeze prevents ANY mods - read only
 *
 */

module.exports = Object.freeze({

  ENVIRONMENTS: {
    PRODUCTION: 'production',
    STAGING: 'staging',
    DEVELOPMENT: 'development',
  },

});
